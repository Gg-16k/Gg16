const {db}=require('./_db');
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const sql=db();if(!sql)return res.status(503).json({error:'Database is not configured yet.'});try{const b=req.body||{};if(!b.name||!b.phone||!b.method||!b.details)return res.status(400).json({error:'Incomplete order'});const r=await sql`INSERT INTO orders(name,phone,contact,payment_method,details,status) VALUES(${b.name},${b.phone},${b.contact||null},${b.method},${b.details},'pending') RETURNING id,status,created_at`;return res.status(201).json(r[0]);}catch(e){return res.status(500).json({error:'Server error'});}};
